<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <link rel="icon" href="/img/user.png">
        <title>{{name}}</title>
        <link rel="stylesheet" href="/css/bootstrap.min.css">
        <link rel="stylesheet" href="/css/styles.css">
    </head>
    <body>
        <div id="fileModal" class="modal fade" role="dialog" tabindex="-1">
            <div class="modal-dialog modal-lg" role="content">
                <div class="modal-content">
                    <div class="modal-header bg-primary text-white">
                        <h3 class="modal-title">Profile Pic</h3>
                        <button type="button" class="close" id="closefileModal">&times;</button>
                    </div>
                    <div class="modal-body">
                        <form action="/users/me/avatar" method="POST" enctype="multipart/form-data">
                            <div class="form-group row">
                                <div class="col-12">
                                    <input type="file" name="avatar" id="avatar" required>
                                </div>
                            </div>
                            <div class="form-group row justify-content-center">
                                <div class="col-sm-auto">
                                    <button type="submit" class="btn btn-primary">Upload</button>
                                </div>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
        {{>navbar}}
        <main>
            <div class="container" style="margin-top: 50px;">
                <div class="card">
                    <h3 class="card-header bg-warning text-white">{{name}}</h3>
                    <div class="card-body">
                        <div class="row">
                            <div class="col-12 col-sm-6">
                                <div class="media">
                                    <img id="profilepic" src="{{avatar}}" alt="{{name}}" class="d-flex mr-3 img-rounded-circle align-self-center" width="250px" height="250px">
                                </div>
                            </div>
                            <div class="col-6">
                                <dl class="row">
                                    <dt class="col-6">ID:</dt>
                                    <dd class="col-6">{{_id}}</dd>
                                    <dt class="col-6">Name:</dt>
                                    <dd class="col-6">{{name}}</dd>
                                    <dt class="col-6">Email:</dt>
                                    <dd class="col-6">{{email}}</dd>
                                </dl>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="container">
                <h4>Your responses</h4>
                {{#if noForm}}
                    <p>You have not filled any forms yet :(... Let's get you started</p>
                    <p>Visit this <a href="/forms">page</a> to start filling out forms</p>
                {{/if}}
                {{#each forms}}
                    <div class="row">
                        <div class="col-12">
                            <div class="card">
                                <h3 class="card-header bg-primary text-white">{{name}}</h3>
                                <div class="card-body">
                                        <dl class="row">
                                            {{#each data}}
                                                <dt class="col-6">{{field}}</dt>
                                                <dd class="col-6">{{value}}</dd>
                                            {{/each}}
                                        </dl>
                                </div>
                            </div>
                        </div>
                    </div>
                {{/each}}
            </div>
        </main>
        <footer class="footer">
            {{>footer}}
        </footer>
        <script src="/js/jquery.slim.min.js"></script>
        <script src="/js/popper.min.js"></script>
        <script src="/js/bootstrap.min.js"></script>
        <script src="/js/profile.js"></script>
    </body>
</html>